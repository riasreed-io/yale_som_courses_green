# Render Blueprint: one Web Service (FastAPI) + one Static Site (React).
#
# Deploy the backend FIRST — the frontend needs its URL for VITE_API_URL.
#
# Values marked `sync: false` are NOT stored in this file and must be set in
# the Render dashboard. Never put PORTKEY_API_KEY or the Supabase connection
# string in the repo.

services:
  # ---------------------------------------------------------------- backend
  - type: web
    name: yale-som-courses-api
    runtime: python
    rootDir: backend
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /api/health
    envVars:
      # Render's default Python can lag; 3.12 is known to build these wheels.
      - key: PYTHON_VERSION
        value: "3.12"
      # Portkey key — set in the dashboard, never here.
      - key: PORTKEY_API_KEY
        sync: false
      # No login gate on the deployed site. Set to "true" to require accounts;
      # the signup/login system stays in the code either way.
      - key: AUTH_REQUIRED
        value: "false"
      # DATABASE_URL is intentionally NOT set: the app falls back to the
      # committed data/yale_som.db (SQLite). Courses always work, but Render's
      # free filesystem is ephemeral, so accounts and chat history reset on
      # every redeploy or sleep/wake. To make those durable later, add a
      # Supabase connection string here and run backend/migrate_to_supabase.py.
      # Signs session tokens. Render generates one; changing it signs everyone out.
      - key: SECRET_KEY
        generateValue: true
      # The static site's URL, e.g. https://yale-som-courses-web.onrender.com
      - key: ALLOWED_ORIGINS
        sync: false

  # --------------------------------------------------------------- frontend
  - type: web
    name: yale-som-courses-web
    runtime: static
    rootDir: frontend
    buildCommand: npm ci && npm run build
    staticPublishPath: dist
    envVars:
      # The backend's URL, e.g. https://yale-som-courses-api.onrender.com
      # Vite bakes this in at BUILD time — change it and you must redeploy.
      - key: VITE_API_URL
        sync: false
    routes:
      # Single-page app: send unknown paths to index.html instead of 404.
      - type: rewrite
        source: /*
        destination: /index.html
